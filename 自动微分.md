# PyTorch 自动微分 (Autograd) 核心要点

## 1. requires_grad - 控制梯度追踪

```python
# 需要求导的变量设置 requires_grad=True
x = torch.ones(2, 2, requires_grad=True)

# 默认不追踪(节省内存)
x2 = torch.ones(2, 2)  # requires_grad=False
```

---

## 2. backward() - 反向传播规则

**关键规则:**
- 只有**标量**才能直接调用 `.backward()`
- 非标量需要先转换: `y.mean()` 或 `y.sum()`

```python
x = torch.ones(2, 2, requires_grad=True)
y = 2 * x + 2

z = y.mean()      # 转为标量
z.backward()       # 执行反向传播

print(x.grad)      # 查看梯度
```

---

## 3. retain_graph - 重复反向传播

**问题:** 默认 `.backward()` 后会释放计算图,再次调用会报错

**解决:** 使用 `retain_graph=True`

```python
z = y.mean()

z.backward(retain_graph=True)  # 保留计算图
z.backward()                    # 可以再次调用
```

---

## 4. torch.no_grad() - 禁用梯度追踪

**用途:** 推理阶段节省内存

```python
with torch.no_grad():
    predictions = model(test_data)  # 不记录梯度
```

---

## 5. 训练循环标准写法

```python
for epoch in range(epochs):
    # 前向传播
    output = model(input)
    loss = criterion(output, target)
    
    # 反向传播
    optimizer.zero_grad()   # ① 清零梯度
    loss.backward()          # ② 计算梯度
    optimizer.step()         # ③ 更新参数
```

---

## 6. 常见错误速查

| 错误 | 原因 | 解决 |
|------|------|------|
| 不能对非标量调用 backward | y 不是单个数值 | 用 `y.mean()` 转标量 |
| 第二次 backward 报错 | 计算图已释放 | 用 `retain_graph=True` |
| 梯度为 None | 没调用 backward | 先执行 `.backward()` |
| 推理时内存占用大 | 记录了不必要的梯度 | 用 `torch.no_grad()` |

---

## 7. 核心API总结

- `requires_grad=True` - 开启梯度追踪
- `.backward()` - 执行反向传播
- `.grad` - 查看梯度值
- `retain_graph=True` - 保留计算图
- `torch.no_grad()` - 禁用梯度追踪
- `zero_grad()` - 清零梯度

---

*简洁版笔记 - 2026年5月*
