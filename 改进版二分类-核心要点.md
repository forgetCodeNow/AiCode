# PyTorch 改进版二分类 - 核心要点

## 📦 1. 使用 Dataset 重构数据

### 核心类：`TensorDataset`

```python
from torch.utils.data import TensorDataset

# 将 Tensor 封装为 Dataset
HRdataset = TensorDataset(X, Y)

# 通过索引访问数据（替代手动切片）
x, y = HRdataset[start:end]
```

**优势：**
- ✅ 自动管理数据索引
- ✅ 代码更简洁，无需手动计算 `start/end`
- ✅ 支持统一的数据访问接口

---

## 🔄 2. 使用 DataLoader 重构数据

### 核心类：`DataLoader`

```python
from torch.utils.data import DataLoader

# 创建 DataLoader（自动分批 + 迭代）
HR_dl = DataLoader(HR_ds, batch_size=64)

# 训练循环简化为直接迭代
for x, y in HR_dl:
    y_pred = model(x)
    loss = loss_fn(y_pred, y)
    opt.zero_grad()
    loss.backward()
    opt.step()
```

**关键特性：**
- ✅ **自动分批**：无需手动计算 `batch_size` 和 `steps`
- ✅ **自动迭代**：直接 `for x, y in loader` 遍历所有批次
- ✅ **支持 shuffle**：`shuffle=True` 打乱数据顺序
- ✅ **支持多进程加载**：`num_workers > 0` 加速数据读取

**对比：**
| 方式 | 代码复杂度 | 灵活性 |
|------|-----------|--------|
| 手动切片 | ⭐⭐ 高 | 低 |
| Dataset | ⭐⭐ 中 | 中 |
| **DataLoader** | ⭐ 低 | **高** ✅ |

---

## ✅ 3. 添加校验（训练/测试分离）

### 核心流程

```python
from sklearn.model_selection import train_test_split

# 1️⃣ 划分训练集和测试集
train_x, test_x, train_y, test_y = train_test_split(
    X_data, Y_data, 
    test_size=0.2,      # 20% 作为测试集
    random_state=42     # 固定随机种子
)

# 2️⃣ 转换为 Tensor
train_x = torch.from_numpy(train_x).type(torch.FloatTensor)
train_y = torch.from_numpy(train_y).type(torch.FloatTensor)
test_x = torch.from_numpy(test_x).type(torch.FloatTensor)
test_y = torch.from_numpy(test_y).type(torch.FloatTensor)

# 3️⃣ 创建 DataLoader
train_dl = DataLoader(TensorDataset(train_x, train_y), 
                      batch_size=64, shuffle=True)
test_dl = DataLoader(TensorDataset(test_x, test_y), 
                     batch_size=128, shuffle=False)
```

### 训练 + 验证循环

```python
# 准确率计算函数
def accuracy(out, yb):
    return ((out.data.numpy() > 0.5) == yb.numpy()).mean()

model, opt = get_model()

for epoch in range(epochs):
    # 🔹 训练模式
    model.train()
    for xb, yb in train_dl:
        pred = model(xb)
        loss = loss_fn(pred, yb)
        opt.zero_grad()
        loss.backward()
        opt.step()

    # 🔸 每10轮验证一次
    if epoch % 10 == 0:
        model.eval()  # 切换到评估模式
        with torch.no_grad():  # 不计算梯度（节省内存）
            val_loss = sum([loss_fn(model(x), y) for x, y in test_dl]) / len(test_dl)
            acc_mean = np.mean([accuracy(model(x), y) for x, y in test_dl])
        print(f"Epoch {epoch} | Val Loss: {val_loss:.4f} | Acc: {acc_mean:.4f}")
```

### 关键点说明

| 概念 | 作用 | 重要性 |
|------|------|--------|
| **`model.train()`** | 启用 Dropout、BN 等训练特有行为 | ⭐⭐⭐ |
| **`model.eval()`** | 关闭 Dropout，使用 BN 统计量 | ⭐⭐⭐ |
| **`torch.no_grad()`** | 禁用梯度计算，节省内存和计算 | ⭐⭐⭐ |
| **`shuffle=True`** | 训练时打乱数据，避免批次偏差 | ⭐⭐ |
| **`shuffle=False`** | 测试时保持顺序，结果可复现 | ⭐ |

---

## 🎯 完整最佳实践模板

```python
# 1. 数据准备
train_dl = DataLoader(TensorDataset(train_x, train_y), batch_size=64, shuffle=True)
test_dl = DataLoader(TensorDataset(test_x, test_y), batch_size=128, shuffle=False)

# 2. 模型、优化器、损失函数
model = HRModel()
opt = torch.optim.Adam(model.parameters(), lr=0.001)
loss_fn = nn.BCELoss()

# 3. 训练循环
for epoch in range(epochs):
    model.train()
    for xb, yb in train_dl:
        pred = model(xb)
        loss = loss_fn(pred, yb)
        opt.zero_grad()
        loss.backward()
        opt.step()
    
    # 4. 定期验证
    if epoch % 10 == 0:
        model.eval()
        with torch.no_grad():
            val_acc = np.mean([accuracy(model(x), y) for x, y in test_dl])
        print(f"Epoch {epoch} | Acc: {val_acc:.4f}")
```

---

## 💡 总结

| 改进点 | 解决的问题 | 收益 |
|--------|-----------|------|
| **Dataset** | 手动切片繁琐 | 代码简洁 |
| **DataLoader** | 分批逻辑复杂 | 自动化 + 高性能 |
| **训练/测试分离** | 过拟合无法检测 | 真实评估泛化能力 |
| **train/eval 模式** | Dropout/BN 行为不一致 | 训练稳定 + 测试准确 |
| **no_grad** | 测试时浪费内存计算梯度 | 节省资源 |

---

*基于 HR.csv 员工离职预测数据集（20个特征）*
